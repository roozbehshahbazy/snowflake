name: Snowflake DCM Check - Test 

on:
  pull_request:
    branches:
      - test
    paths:
      - 'snowflake/**'
      - '.github/workflows/**'

jobs:
  dcm-plan:
    uses: ./.github/workflows/dcm-plan-deploy.yml
    with:
      project_path: ./snowflake
      environment: test
      target: test
      dcm_mode: plan
      allow_object_drop: false
    secrets: inherit